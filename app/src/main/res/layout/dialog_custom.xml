<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/dialogContainer"
    android:gravity="center"
    android:background="@drawable/shape_custom_dialog_bg"
    android:orientation="vertical">
    <!-- 标题 -->
    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="17dp"
        android:layout_marginRight="17dp"
        android:layout_marginTop="25dp"
        android:gravity="center"
        android:lineSpacingExtra="6dp"
        android:textColor="@color/common_text_color"
        android:textSize="18sp"
        android:textStyle="bold"
        tools:text="抱歉，所选班级与当前在读班级上课时间有冲突，请重新选择" />
    <!--Content-->
    <FrameLayout
        android:id="@+id/contentLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="30dp"
        android:layout_marginRight="30dp"
        android:layout_marginTop="12dp"
        android:gravity="center_vertical"
        android:visibility="gone">

        <TextView
            android:id="@+id/content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:gravity="center"
            android:lineSpacingExtra="2dp"
            android:textColor="#999999"
            android:textSize="14sp"
            tools:text="您输入的手机号未注册" />
    </FrameLayout>

    <LinearLayout
        android:id="@+id/operationLayout"
        android:layout_width="match_parent"
        android:layout_height="46dp"
        android:layout_marginBottom="24dp"
        android:layout_marginTop="22dp"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/cancelBtn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@drawable/shape_custom_dialog_cancel_button_bg"
            android:gravity="center"
            android:text="取消"
            android:textColor="@color/color_666666"
            android:textSize="15sp"
            android:textStyle="bold" />

        <View
            android:id="@+id/dividerLine"
            android:layout_width="16dp"
            android:layout_height="0dp" />

        <TextView
            android:id="@+id/confirmBtn"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@drawable/shape_custom_dialog_postive_button_bg"
            android:gravity="center"
            android:text="确定"
            android:textColor="@color/white"
            android:textSize="15sp"
            android:textStyle="bold" />
    </LinearLayout>

</LinearLayout>